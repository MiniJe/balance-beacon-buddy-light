# Configurație pentru aplicația Balance Beacon Buddy

# =============================================================================
# CONFIGURARE BAZA DE DATE
# =============================================================================
DB_HOST=your-sql-server-host
DB_PORT=1433
DB_NAME=your-database-name
DB_USER=your-username
DB_PASSWORD=your-password

# =============================================================================
# CONFIGURARE SERVER
# =============================================================================
PORT=5000
NODE_ENV=production

# =============================================================================
# CONFIGURARE SECURITATE FIȘIERE PDF
# =============================================================================

# 🔒 BLOCARE PDF NESEMNAT în STEP 3 (CRITIC!)
# Controlează dacă să blocheze progresul aplicației când se detectează PDF-uri nesemnate
# true (default) = BLOCHEAZĂ în Step 3 - aplicația NU poate continua cu PDF nesemnat
# false = Permite PDF-uri nesemnate (EXTREM DE NECOMANDAT ÎN PRODUCȚIE!)
BLOCK_UNSIGNED_PDF_FILES=true

# Permite PDF-uri nesemnate DOAR în mediul de dezvoltare (când NODE_ENV=development)
# true = Permite în development pentru testare și debugging
# false (default) = Blochează chiar și în development pentru consistență
ALLOW_UNSIGNED_IN_DEV=false

# ⚠️ IMPORTANT: 
# - Dacă BLOCK_UNSIGNED_PDF_FILES=true și ALLOW_UNSIGNED_IN_DEV=false
#   → Aplicația va bloca ORICE PDF nesemnat în Step 3
# - Această validare se execută ÎNAINTE de Step 4 și Step 5
# - PDF-urile cu hash identic cu originalul sunt considerate NESEMNATE

# =============================================================================
# CONFIGURARE EMAIL
# =============================================================================
EMAIL_HOST=smtp.your-email-provider.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@domain.com
EMAIL_PASS=your-email-password

# =============================================================================
# CONFIGURARE AZURE STORAGE (dacă se folosește)
# =============================================================================
AZURE_STORAGE_CONNECTION_STRING=your-azure-storage-connection-string
AZURE_CONTAINER_NAME=your-container-name

# =============================================================================
# ALTE CONFIGURĂRI
# =============================================================================

# Configurare JWT pentru autentificare
JWT_SECRET=your-jwt-secret-key

# Configurare pentru debugging
DEBUG=false
LOG_LEVEL=info
